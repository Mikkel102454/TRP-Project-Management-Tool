logging:
  level:
    root: info
    org:
      hibernate.SQL: error
      springframework:
        security=DEBUG:

  file:
    path: logs

spring:
  datasource:
    password: TRPnimda10
    url: jdbc:mysql://localhost:3306/pmt
    username: pmt

  jpa:
    hibernate:
      ddl-auto: none

    show-sql: false

    properties:
      hibernate:
        format_sql: true