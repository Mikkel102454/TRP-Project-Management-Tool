package solutions.trp.pmt.controller.api.response;

public enum ApiErrorCode {
    NOT_FOUND,
    UNAUTHORIZED,
    FORBIDDEN,
    VALIDATION_ERROR,
    DATABASE_ERROR,
    CONFLICT,
    INTERNAL_ERROR
}